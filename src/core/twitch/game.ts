export interface Game {
    name: string
    box: Box
    logo: Logo

}

export interface Box {
    small: string
    medium: string
    large: string
    template: string
}

export interface Logo {
    small: string
    medium: string
    large: string
    template: string
}